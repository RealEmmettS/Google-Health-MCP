TT;DR: Wrote three exhaustive onboarding docs — README.md (human front door), CLAUDE.md (Claude session instructions), AGENTS.md (tool-neutral, incl. Codex) — so any agent can pick up the full context, intent, architecture, decisions, and task-board usage without spelunking. All sourced from docs/PLAN.md, the board detail files, and the actual repo.

## Why
Direct operator order (Emmett): "Create a comprehensive and exhaustive README, CLAUDE.md, and AGENTS.md that covers what this is, what purpose it serves, how I intend to use it, how to use the task board and all that stuff — so other agents (even Codex) can easily pick up and understand the context, what it's used for, the intent, and the plan thoroughly." Board stub was tracked as #sgb; completed here under #3m3 per the operator handoff (the #sgb stub was the same task — replaced, not duplicated).

## What was done
- **README.md** (rewritten/expanded): TOC; what-it-is/is-not; who-it's-for + how Emmett uses it (example prompts) + future ideas (#api/#rlw/#w11); ASCII architecture diagram; four-auth-layers table; tech-stack table; coarse board-deferring status; repository-structure tree (with the 15 DB tables); full setup guide (accounts, 6 redirect URIs, 9 health scopes, env-var-by-name table, local dev, deploy); MCP client connection instructions (with a not-live-yet caveat on /api/mcp); MCP surface (9 read / 5+1 write tools / 5 resources); security posture; a troubleshooting table (7-day Testing tokens → publish, unverified-app warning, redirect propagation, reauth_required, Fitbit sync latency, 401, env-not-taking-effect, TS pin, auto-deploy webhook, allowlist rejection); task-board pointer; further reading.
- **CLAUDE.md** (restructured): preserved the full task-management section (subtask rules, completion gates, secrets rule, shared/git-tracked board rules, multi-board port-resolution) and the non-negotiables list; added docs/PLAN.md-is-source-of-truth + current-phase→board pointer, recorded decisions (FastMCP-no / Railway-maybe / built-in mcp plugin / TS pin / #api thin-handlers), watchouts (kebab-vs-snake, civil-vs-physical time, no sleep/exercise/settings writes, redaction, Node runtime, serverless lifecycle, payload bounds, update_profile check), and the command list.
- **AGENTS.md** (new): tool-neutral equivalent — "read these first" onboarding order (PLAN → README → .tasks/CLAUDE.md → board); same non-negotiables/decisions/watchouts/commands framed without Claude-specific skill/plugin references; task-board usage explained plainly (TASKS.md 4-section format, tag meanings, subtasks, detail-file anatomy, completion/waiver gates, id rules, git-tracked-board rule, board server via `node .tasks/board-server.mjs status`); cross-references README/PLAN rather than triplicating depth.

## Constraints honored
Markdown only — touched README.md, CLAUDE.md, AGENTS.md, this board update. No source/config/secret files changed. No git actions. No secret values anywhere (env-var NAMES only; values live in .env.development.local / Vercel env / .tasks/secure/).

## Verification
- [x] README.md covers what/why/intent, architecture (ASCII), status, full setup (accounts, redirect URIs, env vars by name, local dev, deploy), MCP client connect, security, troubleshooting, structure, board pointer — with a TOC
- [x] CLAUDE.md preserves the task-management section (completion gates / subtask / secrets / multi-board rules) + non-negotiables, and adds source-of-truth, phase→board pointer, commands, recorded decisions, watchouts
- [x] AGENTS.md is tool-neutral (no Claude-specific skill/plugin refs), has a read-these-first list, explains the tasks system plainly incl. `node .tasks/board-server.mjs status`, and cross-references rather than triplicates
- [x] Zero secret values in any of the three files (env-var names only; verified against .env.example)
- [x] No stale claims: status is coarse + defers to the board; MCP-endpoint-not-live-yet caveat present; Phase 4 completion race absorbed
- [x] Re-read all three for internal contradictions — none found
- [x] `npx tsc --noEmit` clean (markdown-only change; no-op sanity check)

## Status
DONE. All three docs written and self-consistent. Note for future agents: the docs deliberately keep phase status COARSE and point at the board (`.tasks/TASKS.md`) as authoritative — Phase 4 (#p4g) moved to Done mid-task, which the coarse framing already absorbs. If the repo gains a middleware.ts, a Vercel-locked package, or the src/health-services/ layer, update the "Repository structure" note in README and the decisions/watchouts in CLAUDE.md + AGENTS.md.

## Activity
- 2026-07-09 — created and completed in Done; wrote README.md, CLAUDE.md, AGENTS.md; moved board task to Done (replacing the #sgb stub) (agent: opus-subagent)
</content>
