TT;DR: Qualify, deploy, and prove the complete v1.1 release without confusing build evidence with real Google webhook, connector, or Fitbit acceptance.

## Why

The milestone spans migrations, encryption, auth/offboarding, Google Console configuration, MCP interfaces, and physical device sync. A final qualification task is required because no individual implementation task proves the integrated outcome.

## Scope

Apply migrations, run automated gates, verify destructive operations were correctly scoped, deploy production, register the Google subscriber, run real connector reads/trends/cache checks, trigger a Fitbit sync, verify webhook-to-inbox flow, and record rollback instructions. Better Auth provider migration and outbound notification channels remain separate.

## Plan

Review child evidence; apply migrations before dependent code; deploy fail-closed webhook code; register the subscriber with Emmett's authenticated Google Cloud identity; perform real-device and connector checks; release only after all mandatory gates pass.

## Impact

Moves health.emmetts.dev to the complete single-user v1.1 behavior. Risks include migration mismatch, stale client assumptions, webhook configuration failure, and falsely claiming device-sync latency from server evidence.

## Acceptance

**Functional bar:** production serves the full release to Emmett, Christian is offboarded, cached/trend reads work, and a real Google notification reaches the update inbox.
**Evidence bar:** tests/typecheck/build, migration inspection, production health/auth checks, real connector tool calls, Google subscriber receipt, and one physical Fitbit sync.
**Gate ownership:** automated/deploy gates are repository policy; Emmett owns Google authentication and physical Fitbit acceptance.
**Valid bounded outcomes:** verified, partial awaiting named operator gate, blocked, or not verified.
**Budget / stop rule:** two non-informative real-sync attempts trigger observer/runtime audit; do not loop device sync indefinitely.

## Evidence

| Criterion | Oracle / invocation | Raw result or pointer | Interpretation | Limitation | Status |
|---|---|---|---|---|---|
| Starting automated baseline | `npm test -- --run`; `npm run typecheck` | 83/83 tests; typecheck passed | Existing release starts green | No new work implemented yet | PASS |
| Current automated gates | `npm test -- --run`; `npm run typecheck`; `npm run build`; `npm run db:generate` | 114 tests, typecheck/build pass, no ungenerated schema changes | Source and migration gates are green | Does not prove production webhook delivery | PASS |
| Migration/live Google predeploy | `npm run db:migrate`; `scripts/live-verify.ts`; `scripts/live-verify-v11.ts` | migration applied; all read services pass; all nine trend metrics shaped; repeat cache-only; 0 plaintext/incomplete cipher | Shared DB and Emmett connection are compatible before deploy | Local runtime against production data | PASS |
| Dependency audit | `npm audit --omit=dev --audit-level=high`; `npm ls next postcss sharp` | exit 0; Next 16.2.11, PostCSS 8.5.23, Sharp 0.35.3 | No high/critical production finding remains | Moderate Hono Windows static-file path is unused on Vercel Linux; Drizzle esbuild loader is local-development-only | PASS |
| Google webhook type reconciliation | `projects.subscribers.create`; current Google webhook support list | API rejected `total-calories`; current list contains 27 supported types | Registry corrected before subscriber retry | Google's support matrix may change again | PASS |
| Production deploy and perimeter probes | Vercel deploy `dpl_EEoZhjctsbh1nL5vtYCLAs3jr4Nh`; HTTP probes | commit `398fb99`; health/privacy/metadata 200; unauthenticated MCP/webhook/cron 401; authorized webhook handshake and cron 200 | Corrected registry commit deployed with fail-closed auth and maintenance route active | Does not prove a Google data-change notification | PASS |
| Authenticated production MCP | existing unexpired Emmett connector token; `get_sync_status`, `get_health_trends`, `get_health_updates` | MCP 0.2.0; force-refresh source=live; repeat source=cache; steps 7/7, sleep 6/7; empty inbox readable | Emmett-only continuity, trends, bypass, cache, and inbox interface pass in production | Empty inbox is expected until a post-registration data change | PASS |
| Google subscriber | `projects.subscribers.create` then `list` | `shaughv-health-mcp`; AUTOMATIC; 27 types; endpoint and secret set; operation done | Google accepted both verification challenges and activated delivery | No physical Fitbit update observed yet | PASS |

## Verification

- [ ] All child tasks have closed Verification and PASS evidence
- [x] Production migration, health, OAuth metadata, and MCP authorization probes pass
- [x] Emmett completes live cache, force-refresh, trends, and update-inbox connector reads
- [ ] Google subscriber handshake succeeds and one physical Fitbit sync reaches the inbox
- [ ] Rollback and data-deletion procedures are exercised or safely dry-run

## Status

TO-DO. Source, migration, production, connector, and Google subscriber gates are green. The only live-system gate is one new Fitbit/Health data change reaching the webhook and MCP inbox.

## Activity

- 2026-07-25 04:05 — created as the milestone's required integrated qualification child (agent: codex)
- 2026-07-25 11:00 — recorded 114-test/type/build/schema gates, applied migration, and passed real Google/cache/trend predeployment checks (agent: codex)
- 2026-07-25 11:18 — patched Next/PostCSS/Sharp high-severity advisories; accepted and bounded the two remaining moderate dependency advisories without using audit's unsafe forced downgrades (agent: codex)
- 2026-07-25 11:27 — live subscriber creation identified Google documentation drift for `total-calories`; reconciled the registry to the current 27-type webhook support list before retrying (agent: codex)
- 2026-07-25 11:34 — deployed the release, passed production perimeter probes and authenticated MCP cache/trend/update reads, registered the 27-type subscriber, and confirmed Google Audience has one production user with no Christian test-user state (agent: codex)
- 2026-07-25 11:42 — redeployed corrected commit `398fb99` as `dpl_EEoZhjctsbh1nL5vtYCLAs3jr4Nh`; health and subscriber rechecks pass, while event/inbox/freshness counts remain zero pending a genuine post-registration Fitbit data change (agent: codex)
