TT;DR: v1.1 — wire Google Health webhooks so the server learns when new data lands: service account + IAM, subscriber registration, signature verification, idempotent event storage, and the data_freshness ledger.

## Why
Deferred from v1 by explicit decision (2026-07-09): for on-demand Q&A, live fetches already give correct answers; webhooks mainly enable future proactive features (morning check-ins, "new data arrived" triggers) and richer freshness metadata. Emmett's ask: track latest notification per (account, data type) — NOTE webhook payloads carry NO values, only `{healthUserId, dataType, operation, intervals}` pointers, so the ledger stores notification metadata, not health values.

## Plan
Per docs/PLAN.md Phase 8: use Emmett's authenticated Cloud identity for one-time subscriber management unless IAM requires a dedicated service account; use the project NUMBER (not ID) in subscriber API paths; register an AUTOMATIC subscriber for granted-scope data types; implement `app/api/webhooks/google-health/route.ts` with auth-secret check, two-part verification handshake (200/201 authed verification, 401/403 unauthed), GOOGLE-HEALTH-API-SIGNATURE verification vs Google's public keyset (Tink prefix → ECDSA P-256, keyset cached, rotation-safe), idempotent insert into webhook_events (payload hash), data_freshness upsert, overlapping encrypted-cache invalidation, and proactive-inbox enqueue. Durable writes happen before the 204 response; no health fetch depends on post-response execution.

## Impact
New public endpoint; new GCP IAM surface. Retry semantics: Google stores undelivered notifications 7 days.

## Acceptance
Handshake passes on prod; a real Fitbit sync produces an event row + fresh ledger entry; duplicates don't double-insert.

## Verification
- [x] Mocked: authed verification 200; unauthed 401; bad signature 403; exact raw-body P-256 verification and rotation refresh
- [ ] Duplicate → single row; unknown healthUserId stored unresolved
- [x] Prod: subscriber registered; Google handshake passes
- [ ] Real sync → webhook_events row + data_freshness upsert
- [ ] MCP freshness metadata surfaces last_notified_at when present

## Status
ACTIVE. Production endpoint and the 27-type AUTOMATIC Google subscriber are live and handshake-verified. A real Fitbit data change remains the final end-to-end delivery gate.

## Activity
- 2026-07-09 00:15 — created as deferred v1.1 scope during planning (agent: fable)
- 2026-07-25 04:05 — Emmett green-lit v1.1; moved Backlog → To-Do, linked to #v11/#fsp, and added cache invalidation plus proactive-inbox obligations (agent: codex)
- 2026-07-25 11:00 — implemented authorized handshake, raw-body Tink/P-256 verification against Google's rotating public keyset, idempotent pointer persistence, ledger/inbox writes, and data-type cache invalidation; production secrets/project identifiers configured (agent: codex)
- 2026-07-25 11:29 — deployed production and registered `shaughv-health-mcp` with AUTOMATIC policy for all 27 currently supported webhook types; Google's authorized and unauthorized verification handshake passed and the subscriber lists active with its secret set (agent: codex)
