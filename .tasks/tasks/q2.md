TT;DR: Qualify the integrated 0.3.0 release across migrations, OAuth/JWT security, Google DPoP, every active MCP connector, production UI, rollback, and the time-based soak without conflating those gates.

## Why

Automated tests cannot prove connector reauthorization, a Google-issued DPoP-bound refresh token, browser consent usability, or seven days of production behavior. This task owns those integrated gates and keeps operator/time evidence distinct from implementation evidence.

## Scope

Migration rehearsal and rollback; production schema counts; 0.2.1 and 0.3.0 deploy evidence; ChatGPT, Claude.ai, Claude Code, Codex, and Cursor authorization/discovery/ping/live-read checks; work-workspace Claude propagation; Google prompt-consent DPoP replacement and refresh; responsive/accessibility/font QA; logs, latency, protocol-era and Neon-query monitoring; seven-day legacy-table retention and cleanup. The separate real Fitbit webhook delivery gate remains owned by `#w11/#q11`.

## Plan

Run every automated and safe production gate first; ask Emmett only for connector or Google consent actions that cannot be performed without his browser/account choice; monitor 24 hours and seven days; delete legacy auth rows/tables only after zero required rollback use and explicit acceptance.

## Impact

Completing this task makes 0.3.0 the sole forward path and removes the legacy OAuth rollback store. Until then, rollback remains available by restoring the prior Vercel deployment.

## Acceptance

Every named connector passes auth/discovery/ping/non-mutating read, Google DPoP refresh succeeds without losing the prior token on failure, production remains clean through the soak, and legacy OAuth cleanup is evidenced without exporting token contents.

## Verification

- [ ] Migration rehearsal and rollback pass without a paid Neon branch
- [ ] 0.2.1 and 0.3.0 production artifacts and rollback targets are recorded
- [ ] ChatGPT, Claude.ai, Claude Code, Codex, and Cursor pass the connector matrix
- [ ] Intended work Claude workspace and Desktop/Code propagation close `#cau`
- [ ] Google DPoP reconsent, refresh, nonce handling, and subsequent live read pass
- [ ] 390/768/1440, keyboard, touch, reduced-motion, redirect, font, and overflow QA pass
- [ ] 24-hour and seven-day observability gates pass
- [ ] Legacy OAuth data is securely removed only after the rollback window

## Status

TO-DO. This task begins after `#sdk2`, `#oap`, and `#font2` meet their implementation gates; operator-owned reconnects and time-based soak cannot be pre-claimed.

## Activity

- 2026-07-29 01:45 - Created as the integrated release/evidence owner; explicitly preserved the unrelated physical Fitbit notification gate. (agent: codex)
